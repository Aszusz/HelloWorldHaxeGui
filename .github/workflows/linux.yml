name: Haxe Workflow Linux Demo
  
on:
  workflow_dispatch
  
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    env:
      MY_VAR: ${{ vars.USE_BIN_CACHE }}
    steps:
    - name: Use Cache
      if: env.MY_VAR
      run: echo "caching $MY_VAR"

    - name: Use No Cache
      if: env.MY_VAR
      run: echo "no caching $MY_VAR"
