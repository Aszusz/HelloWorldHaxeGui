name: Haxe Workflow Linux Demo
  
on:
  workflow_dispatch
  
env:
#   USE_BIN_CACHE: true
  
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    
    steps:
    - name: Use environment variables from entire job
      run: echo We display the $USE_BIN_CACHE define for the entire job1

    - name: Use environment variables from entire job
      run: echo We reuse the output suffix $USE_BIN_CACHE multiples time in the entire job1

