name: Haxe Workflow Linux Demo
  
on:
  workflow_dispatch
  
env:
  USE_BIN_CAHCE: true
  
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    
    steps:
      - run: echo ${{ USE_BIN_CAHCE }}
